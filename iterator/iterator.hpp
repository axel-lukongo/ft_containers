#ifndef ITERATOR_HPP
#include <iostream>

template <typename vector>
class iterator
{
private:
	/* data */
public:
	iterator(/* args */){}
	iterator(iterator & copy){}
	iterator& operator=(iterator & copy){}
	~iterator(){}
};

#endif // !ITERATOR_HPP